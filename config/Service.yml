apiVersion: v1
kind: Service
metadata:
    name: app1-service
spec:
    selector:
        app: wil42
    ports:
        - name: http
          port: 8888
          targetPort: 8888
    type: ClusterIP